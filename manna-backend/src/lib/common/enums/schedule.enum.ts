export enum TimeUnit {
  DAY = 'DAY',
  HOUR = 'HOUR',
  MINUTE = 'MINUTE',
}

export enum ScheduleType {
  INDIVIDUAL = 'INDIVIDUAL',
  COMMON = 'COMMON',
}

export enum MeetingType {
  OFFLINE = 'OFFLINE',
  ONLINE = 'ONLINE',
  NONE = 'NONE',
}
